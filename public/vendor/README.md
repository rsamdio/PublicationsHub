# Vendored reader libraries

Static copies of pdf.js and StPageFlip for same-origin loading (see `lib/client/viewer.js`).

To upgrade: install/copy the new version into a new folder (e.g. `pdfjs/4.x.x/`), update the version constants in `viewer.js`, then remove the old folder.
